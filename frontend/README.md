# React Frontend

Vite-powered React dashboard for the stock broker assignment.

```bash
npm install
npm run dev
```

Open `http://localhost:5173` while the backend is running on `http://localhost:4000`.

Production: `https://stock-broker-client-assessment.vercel.app`
